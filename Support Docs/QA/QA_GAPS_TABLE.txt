╔════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╗
║                              ARA QA AUDIT — GAP PRIORITY MATRIX                                                            ║
║                                  15 Critical + 12 High + 8 Medium                                                           ║
╚════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╝

CRITICAL GAPS (Must fix before Phase 1 launch)
══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

ID   Gap                                           Severity   Effort  Blocker?  Test Outline
──   ────────────────────────────────────────────  ─────────  ──────  ───────  ────────────────────────────────────────────
C1   Full E2E pipeline: scout→writer→outputs       CRITICAL   3d      YES      Execute all 7 agents end-to-end on "immigrants
     (V1 success criteria)                                                     sweden" query. Verify papers→claims→hypotheses
                                                                               →paper.md generated. Output files valid.

C2   All 9 search APIs fail / partial failure      CRITICAL   2d      YES      Mock all 9 APIs timeout. Verify search_all 
                                                                               returns empty + error. Don't crash. Test 5 
                                                                               succeed/4 fail scenario.

C3   Database concurrent access (threading safety) CRITICAL   2d      YES      Threads: 1) search_all stores papers, 2) 
                                                                               extract_claims on same session. No locks, no
                                                                               corruption. Race condition test.

C4   Subtask cancellation (nested agents)          CRITICAL   1.5d    YES      User cancels at depth 2. Parent AND child 
                                                                               stop. Current: only parent checks flag.

C5   Budget cap enforcement & warning gates        CRITICAL   1.5d    YES      Cost reaches 80%→warning shown. Cost reaches
                                                                               100%→pipeline pauses. User can increase cap.

C6   Tool names in prompts vs TOOL_DEFINITIONS     CRITICAL   1d      YES      Prompt says "call search_arxiv()". If prompt
                                                                               uses wrong name, model calls non-existent tool.

C7   Extract claims tool end-to-end (LLM call)    CRITICAL   1.5d    YES      extract_claims marked "LLM-powered" but tool
                                                                               untested with actual model. Missing path?

C8   Verification tools (retraction/cite/DOI)     CRITICAL   2d      YES      check_retraction, get_citation_count, 
                                                                               validate_doi completely untested. 0% coverage.

C9   Output file generation & format validation    CRITICAL   2d      YES      paper.md, paper.html, index.html, .bib must
                                                                               exist. Format valid (markdown, HTML, BibTeX).

C10  Branch search tool untested (Brancher agent)  CRITICAL   1.5d    YES      branch_search marked untested. Cross-domain
                                                                               search never executed. Tool returns?

C11  Approval gate TUI flow (user interaction)    CRITICAL   2d      YES      User approves/rejects/edits at each gate. Gate
                                                                               files written (scout_results.md, etc.). Real
                                                                               Rich TUI rendering.

C12  Hypothesis generator loop (reject→retry)     CRITICAL   1.5d    YES      Critic rejects hyp. Manager retries with 
                                                                               feedback. Loop 3x max. If all reject, show
                                                                               "negative result".

C13  Empty search results handling                 CRITICAL   1d      YES      search_all finds 0 papers. Analyst has no
                                                                               data. Gate shows "0 papers". User can refine.

C14  Rate limit retry + propagation               CRITICAL   1.5d    YES      Gemini 429→retry with backoff. After max
                                                                               retries→RateLimitError. Engine catches, stops
                                                                               pipeline gracefully.

C15  Writer full pipeline (outline→draft)         CRITICAL   2.5d    YES      Outline approved. Full draft written section
                                                                               by section. get_citations retrieves all refs.
                                                                               Final approval. Output files created.

─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
CRITICAL SUBTOTAL: 15 gaps × avg 1.7d = ~25 days (with parallelization: ~10-13 days with 2 engineers)


HIGH PRIORITY GAPS (Should fix in Phase 1 QA cycle)
══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

ID   Gap                                           Severity   Effort  Context
──   ────────────────────────────────────────────  ─────────  ──────  ────────────────────────────────────────────────────
H1   Invalid API key handling                      HIGH       0.5d    Bad GOOGLE_API_KEY→auth fail→user error message
                                                                       Model.generate raises ModelError. Caught, shown.

H2   Gemini model error (not rate limit)          HIGH       0.5d    Model.generate raises ModelError (bad request).
                                                                       Engine catches→returns error JSON. Pipeline stops.

H3   Long-running tool cancellation (fetch_pdf)  HIGH       1d      fetch_fulltext from Unpaywall slow. Set cancel
                                                                       flag. Does it interrupt? NO—blocks. Add timeout.

H4   Embedding tool failure (embed_text)         HIGH       0.5d    embed_text calls Gemini. If key invalid or API
                                                                       down, returns error. Research continues or halts?

H5   Session resume validation                    HIGH       1d      Create session→close→reopen. verify all data
                                                                       matches. DB session_id lookup. State restored.

H6   Loop detection (cache + dedup edge case)    HIGH       1d      Identical calls across 2+ turns. Cache prevents
                                                                       re-execution. Verify no duplicate API calls.

H7   Prompt injection via user input              HIGH       0.5d    User input with special chars/newlines. Escaping
                                                                       correct? Prompt not broken. No injection.

H8   Concurrent subtask threads                   HIGH       1.5d    Manager spawns 2+ subtasks in parallel (Brancher
                                                                       + Hypothesis?). No race conditions. Safe threads.

H9   Cost tracking across all models              HIGH       0.5d    Use multiple LLM providers (Gemini, etc.). Costs
                                                                       tracked separately. Totals sum correctly.

H10  Gate data files (scout_results.md, etc.)    HIGH       1d      request_approval writes gate files. Valid markdown
                                                                       format. Tables, lists render. Not corrupted.

H11  Rule injection into phase prompts            HIGH       1d      Rules added to session. Generated prompts include
                                                                       them. Verify format correct. Agent respects.

H12  Paper deduplication edge cases               HIGH       0.5d    Same title/diff DOI. Same DOI/diff title. Empty
                                                                       title. Dedup logic correct for all cases.

─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
HIGH SUBTOTAL: 12 gaps × avg 0.75d = ~9 days (sequential: ~9 days)


MEDIUM PRIORITY GAPS (Can defer to Phase 2, but improve robustness)
══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

ID   Gap                                           Severity   Effort  Notes
──   ────────────────────────────────────────────  ─────────  ──────  ─────────────────────────────────────────────
M1   search_all concurrency (parallel vs seq)     MEDIUM     1d      Concurrent vs sequential execution mode unclear.
                                                                       Measure wall-clock time with slow APIs.

M2   Empty abstracts in papers                    MEDIUM     0.5d    Store paper no abstract. extract_claims handles?
                                                                       Should return error or instruction.

M3   Vector search (sqlite-vec) integration       MEDIUM     1d      Papers + claims have embedding columns. Search by
                                                                       vector. Similarity matching works. K-NN correct.

M4   Paper full-text caching (PDFs)               MEDIUM     1d      PDFs cached to .ara/papers/. Verify not
                                                                       re-fetched. Path tracking correct.

M5   Settings persistence (default_model, etc.)   MEDIUM     0.5d    Change setting. Close. Reopen. Persisted?
                                                                       JSON file correctly saved/loaded.

M6   Config override precedence (CLI>env>proj>gl) MEDIUM     0.5d    CLI flag overrides env var. Env overrides 
                                                                       project config. Project overrides global.

M7   TUI rendering (Rich tables, panels, etc.)    MEDIUM     1d      Approval gates show tables, progress bars. Hard to
                                                                       test without snapshot testing.

M8   Citation style variations (APA7, Chicago)    MEDIUM     1d      Different citation styles produce different
                                                                       formats. BibTeX correct for each.

─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
MEDIUM SUBTOTAL: 8 gaps × avg 0.8d = ~6-7 days


RISK MATRIX
════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

Risk Level    Count   Total Days   Blockers?   Can Defer?   Recommendation
─────────────  ────   ──────────   ─────────   ──────────   ────────────────────────────
CRITICAL       15     25 days      YES (all)   NO           FIX FIRST (10-13 days w/ 2 eng)
HIGH           12     9 days       NO          SOME         FIX IN PHASE 1 QA (parallel)
MEDIUM         8      6-7 days     NO          YES          PHASE 2 (defer 2-3 items)
─────────────────────────────────────────────────────────────────────────────────────────
TOTAL          35     40-41 days              13-15 days critical path


EFFORT ESTIMATION (WITH 2 QA ENGINEERS IN PARALLEL)
════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

Engineer 1 (Integration/E2E):         Engineer 2 (Unit/Edge Cases):
  C1 (E2E pipeline)         3d          C5 (Budget)               1.5d
  C2 (Search failures)      2d          C6 (Tool validation)      1d
  C3 (DB concurrent)        2d          C7 (Extract claims)       1.5d
  C11 (Gates flow)          2d          C8 (Verification tools)   2d
  C15 (Writer pipeline)     2.5d        C4 (Cancellation)         1.5d
  C13 (Empty results)       1d          C9 (Output files)         2d
  ────────────────────────              C10 (Branch search)       1.5d
  Total: ~12.5 days                     C12 (Hypothesis loop)     1.5d
                                        H1-H12 (High priority)    ~6d
                                        ────────────────────────
                                        Total: ~15 days

PARALLEL SCHEDULE: ~15 days to complete all critical + high priority gaps
SEQUENTIAL: ~34 days


PHASE 1 READINESS SCORECARD
════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

Criterion                                    Status   Score  Notes
───────────────────────────────────────────  ───────  ─────  ─────────────────────────────────────
✓ Engine core logic works                    PASS     85%    Tool dispatch, token counting, cancel flag
✓ Database schema + CRUD                     PASS     85%    All operations work; vector search untested
⚠ Search API integration                     PARTIAL  35%    No real API tests; failure modes untested
⚠ Tool implementations                       PARTIAL  50%    Some tools work; critical tools untested
❌ Full pipeline end-to-end                   FAIL     0%     Never executed all 7 agents together
❌ Approval gate TUI                          FAIL     20%    Gate logic exists; user interaction untested
❌ Output file generation                     FAIL     5%     No code visible for output file creation
❌ Error path testing                         FAIL     15%    Few error scenarios tested
❌ Concurrent access safety                   FAIL     0%     No synchronization testing
─────────────────────────────────────────────────────────────────────────────────────────────────
OVERALL PHASE 1 READINESS: 40-45% COMPLETE FOR USER-FACING LAUNCH

RECOMMENDATION: Fix critical gaps (13-15 days) before launch


DETAILED GAP BREAKDOWN BY SOURCE FILE
════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

File                           Lines   Tests   Coverage   Critical Gaps
──────────────────────────────────────────────────────────────────────────────────────────────────
ara/engine.py                  483     12      40%        Loop detection tested but edge cases not;
                                                           Subtask depth 2+ untested; Cancel flag not
                                                           inherited by subtasks.

ara/tools/search.py            631     3       5%         ALL 9 APIs untested. Rate limit retry
                                                           untested. Partial failure scenarios untested.
                                                           search_all concurrency unclear.

ara/tools/papers.py            128     6       40%        fetch_fulltext not tested with slow APIs.
                                                           search_similar keyword matching only, no
                                                           vector search test.

ara/tools/verification.py      97      0       0%         ZERO tests. check_retraction, get_citation_count,
                                                           validate_doi completely untested.

ara/tools/research.py          109     2       15%        extract_claims "LLM-powered" but not tested
                                                           with model. score_hypothesis stores but untested
                                                           end-to-end. branch_search untested.

ara/tools/writing.py           78      2       25%        write_section only basic test. get_citations
                                                           tested but full paper generation path untested.

ara/tools/pipeline.py          100     3       30%        request_approval auto-approves in test. Real
                                                           gate flow (user input, markdown files) untested.
                                                           Budget enforcement missing.

ara/db.py                      471     10      35%        CRUD works. Dedup logic tested. But vector
                                                           search untested. Concurrent access untested.

ara/model.py                   322     10      35%        Gemini integration exists. EchoFallback works.
                                                           But rate limit retry logic not tested with real
                                                           429 responses.

ara/runtime.py                 95      6       40%        Session creation + resume partially tested.
                                                           Full lifecycle untested.

ara/prompts/                   ~400    7       5%         Prompts built but never executed. Tool names
                                                           unvalidated. Phase transitions untested.

ara/tui.py                     263     10      25%        Slash commands tested. Rich rendering not
                                                           tested. User input flow untested.

─────────────────────────────────────────────────────────────────────────────────────────────────
TOTAL SOURCE: 3,800+ lines    104 tests   ~28% AVG     ~35 gaps across all layers


KEY STATISTICS
════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

Metric                                Value        Target      Status
────────────────────────────────────────────────────────────────────────────────────────────────
Total Test Count                      104          >300        ⚠️  LOW (need 3x more)
Critical Gap Tests                    0            15          ❌ ZERO (all 15 missing)
Integration Tests (tools + DB)        27           >50         ⚠️  INSUFFICIENT
Full E2E Pipeline Tests               0            3+          ❌ CRITICAL (0/3)
Error Path Tests                      ~5           >40         ⚠️  VERY LOW
Concurrent Access Tests               0            5+          ❌ ZERO (high risk)
Test Coverage Density                 2.7%         >70%        ❌ CRITICALLY LOW
Files with 0% Coverage                3            0           ❌ BAD (verify.py, research.py, writing.py)
Average Tests per Source File         2.7          4           ⚠️  BELOW TARGET

────────────────────────────────────────────────────────────────────────────────────────────────
Overall: Phase 1 code complete but QA incomplete. Recommend 13-15 days of focused testing
before user-facing launch to address critical + high-priority gaps.
════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════
